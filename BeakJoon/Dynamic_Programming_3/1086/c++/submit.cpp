#include <iostream>
#include <vector>
#include <algorithm>
#include <limits>

int n;
int k;

std::vector<int> nums;

int solve(int k){

}

int main(){
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    std::ios_base::sync_with_stdio(false);

    std::cin >> n;
    nums.assign(n, 0);

    for(int i = 0; i < n; i++)[
        std::cin >> nums[i];
    ]

    std::cin >> k;

    std::cout << solve(?) << std::endl;

    return 0;
}