#include <iostream>
#include <vector>
#include <algorithm>
#include <utility>
#include <limits>

int main(){
    std::cin.tie(nullptr); std::cout.tie(nullptr); std::ios_base::sync_with_stdio(false);
    int n;
    std::cin >> n;

    int five = 0, two = 0;
    for(int i = 2; i <= n; i++){
        int temp = i;
        while(
    }

    std::cout << std::min(a, b) << "\n";
    return 0;
}